[package]
name = "castify-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "castify-cli"
path = "src/main.rs"

[dependencies]
castify = { path = "..", default-features = false, features = ["cli"] }
